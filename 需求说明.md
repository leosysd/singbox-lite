# SingBox Lite OpenWrt LuCI App 完整需求说明

## 1. 项目目标

`SingBox Lite` 是一个运行在 OpenWrt LuCI 里的 sing-box 管理 App。

它的目标不是替代 OpenClash、HomeProxy、Nikki 这类完整代理面板，而是把 sing-box 最基础、最常用、最容易出错的配置导入和运行管理做好。

核心需求：

1. 支持导入本地 sing-box JSON 配置。
2. 支持导入远程 URL sing-box JSON 配置。
3. 支持选择运行模式，例如 `sing-box + mosdns`。
4. 支持配置检查、备份、应用和回滚保护。
5. 支持查看运行状态和日志。
6. 支持简单的自动更新和日志清理。

## 2. 项目定位

项目类型：

```text
OpenWrt LuCI App
```

建议包名：

```text
luci-app-singbox-lite
```

LuCI 显示名：

```text
SingBox Lite
```

菜单位置：

```text
服务 -> SingBox Lite
```

适用环境：

```text
OpenWrt
LuCI
sing-box
可选 MosDNS
```

当前目标路由器环境：

```text
OpenWrt target：x86/64
sing-box 程序：/usr/bin/sing-box
sing-box 服务：/etc/init.d/sing-box
sing-box 配置：/etc/sing-box/config.json
sing-box 日志：/etc/sing-box/sing-box.log
MosDNS 服务：/etc/init.d/mosdns
MosDNS 默认监听：127.0.0.1:5335
LuCI JS 页面目录：/www/luci-static/resources/view
rpcd ACL 目录：/usr/share/rpcd/acl.d
```

## 3. 第一版功能范围

第一版必须完成：

- LuCI 菜单入口。
- 总览页。
- 本地 JSON 配置导入。
- 远程 URL JSON 配置导入。
- 配置检查。
- 配置备份。
- 配置应用。
- sing-box 服务启动、停止、重启。
- MosDNS 服务重启。
- 运行模式选择。
- 日志查看。
- 日志清理。
- 日志自动清理设置。
- 远程配置自动更新设置。
- 基础状态显示。
- 错误提示。

第一版可以不做：

- 复杂订阅解析。
- 多节点策略组编辑。
- 节点测速排序。
- 流量统计图表。
- 规则集编辑器。
- 按设备分流。
- GeoIP/Geosite 规则可视化管理。
- Clash API 面板。
- 内置 Web Dashboard。

## 4. 总体架构

推荐采用现代 LuCI 架构：

```text
LuCI JS 前端
  -> rpcd/ubus 调用
    -> ucode 后端脚本
      -> sing-box / init.d / 文件系统 / cron
```

不建议把所有逻辑写在旧式 Lua controller 里。

建议目录：

```text
luci-app-singbox-lite/
  Makefile
  root/
    etc/
      config/
        singboxlite
    usr/
      lib/
        lua/
          luci/
            controller/
              singboxlite.lua
      share/
        rpcd/
          acl.d/
            luci-app-singbox-lite.json
        ucode/
          singboxlite.uc
        singboxlite/
          config-template.uc
          helpers.uc
    www/
      luci-static/
        resources/
          view/
            singboxlite/
              overview.js
              ruleset.js
              import.js
              mode.js
              logs.js
```

## 5. 页面结构

LuCI 页面使用 3 个标签页：

```text
总览
规则集
日志
```

总览页保留配置导入、运行模式、远程配置和 MosDNS 联动。规则集放第二页。日志中心放第三页，并参考 GFSingBox 的日志中心交互。

页面风格：

- 保持 LuCI 管理后台风格。
- 白色和浅灰背景。
- 蓝色作为主操作按钮颜色。
- 绿色表示运行正常。
- 黄色表示警告。
- 红色表示错误。
- 不做宣传页。
- 不做复杂大屏。
- 不做花哨背景。
- 控件密度适中，路由器后台优先。

## 6. 总览页

总览页用于让用户一眼看到当前状态，并执行常用操作。

### 6.1 顶部状态条

显示 4 个状态项：

```text
sing-box：运行中 / 未运行
配置：/etc/sing-box/config.json
DNS：MosDNS 127.0.0.1:5335
模式：sing-box + mosdns
```

如果 MosDNS 未安装或未运行：

```text
DNS：MosDNS 未运行
```

如果 sing-box 未安装：

```text
sing-box：未安装
```

### 6.2 操作工具栏

按钮：

```text
检查配置
备份配置
重启 sing-box
重启 mosdns
清理日志
```

可选按钮：

```text
启动 sing-box
停止 sing-box
应用待应用配置
```

### 6.3 左侧主操作区

显示两个导入入口：

```text
本地 JSON 配置
远程 URL 配置
```

这里不展开复杂配置项，只保留最重要操作。

### 6.4 右侧运行概况

显示状态块：

```text
服务状态
TUN 透明代理
DNS/MosDNS
配置检查
最近日志
```

示例字段：

```text
sing-box：running
PID：23341
TUN：singtun0
auto_route：开启
auto_redirect：开启
strict_route：开启
DNS：127.0.0.1:5335
配置检查：通过，未应用
```

## 7. 配置导入页

配置导入是本 App 的核心页面。

页面分成两个主要区域：

```text
本地 JSON 配置
远程 URL 配置
```

## 8. 本地 JSON 配置导入

### 8.1 页面字段

```text
本地 JSON 配置
选择 config.json
上传并检查
应用配置
支持 sing-box 原生 JSON
```

### 8.2 操作流程

完整流程：

```text
用户选择本地 JSON 文件
上传到临时路径
检查文件是否为空
检查是否为合法 JSON
执行 sing-box check
显示检查结果
用户点击应用配置
备份当前配置
替换正式配置
重启 sing-box
刷新状态
```

### 8.3 临时文件路径

建议：

```text
/tmp/singboxlite/import-local.json
```

### 8.4 检查命令

```sh
/usr/bin/sing-box check -c /tmp/singboxlite/import-local.json
```

### 8.5 应用前要求

必须满足：

```text
文件存在
文件非空
JSON 合法
sing-box check 通过
```

不满足则禁止应用。

## 9. 远程 URL 配置导入

### 9.1 页面字段

```text
远程 URL 配置
URL 输入框
拉取并检查
应用配置
自动更新
更新时间
```

URL 示例：

```text
https://example.com/sing-box.json
```

### 9.2 操作流程

完整流程：

```text
用户输入远程 URL
点击拉取并检查
后端下载到临时路径
检查 HTTP 状态
检查文件是否为空
检查是否为合法 JSON
执行 sing-box check
显示检查结果
用户点击应用配置
备份当前配置
替换正式配置
重启 sing-box
刷新状态
```

### 9.3 临时文件路径

建议：

```text
/tmp/singboxlite/import-remote.json
```

### 9.4 下载工具优先级

后端可按顺序尝试：

```text
uclient-fetch
wget
curl
```

下载时应设置超时。

建议超时：

```text
30 秒
```

### 9.5 自动更新

如果开启自动更新，保存：

```text
远程 URL
更新时间
是否自动应用
上次更新时间
上次更新结果
```

自动更新流程也必须执行 `sing-box check`。

自动更新不能直接覆盖正式配置。

## 10. 运行模式页

运行模式页用于决定 sing-box 与 DNS 组件的协作方式。

### 10.1 模式列表

```text
sing-box + mosdns
仅 sing-box DNS
仅导入配置
```

### 10.2 sing-box + mosdns 模式

这是当前推荐模式。

说明：

```text
sing-box 负责代理和 TUN。
MosDNS 负责 DNS。
sing-box DNS 指向 MosDNS。
sing-box 不主动劫持 DNS。
```

默认参数：

```text
MosDNS 地址：127.0.0.1
MosDNS 端口：5335
禁用 DNS 劫持：开启
应用配置后重启 MosDNS：开启
```

如果存在脚本：

```text
/usr/bin/sing-box-disable-dns-hijack
```

则在 sing-box 重启后可调用：

```sh
/usr/bin/sing-box-disable-dns-hijack
```

### 10.3 仅 sing-box DNS 模式

说明：

```text
sing-box 负责代理和 DNS。
不依赖 MosDNS。
不调用 MosDNS 重启。
不调用禁用 DNS 劫持脚本。
```

### 10.4 仅导入配置模式

说明：

```text
只负责导入、检查、备份和应用 JSON。
不主动修改 DNS 行为。
不对配置做额外重写。
```

该模式适合用户自己维护完整 sing-box JSON。

## 11. 日志页

### 11.1 显示内容

显示：

```text
日志路径
日志大小
最近日志
ERROR 过滤
FATAL 过滤
```

默认日志：

```text
/etc/sing-box/sing-box.log
```

### 11.2 操作按钮

```text
刷新日志
清理日志
只看 ERROR
只看 FATAL
下载日志
```

下载日志为可选功能。

### 11.3 清理日志

清理命令：

```sh
/usr/bin/truncate -s 0 /etc/sing-box/sing-box.log
```

### 11.4 自动清理日志

建议 cron：

```cron
10 3 * * * /usr/bin/truncate -s 0 /etc/sing-box/sing-box.log # clear sing-box log daily
```

UI 字段：

```text
每天自动清理日志：开启 / 关闭
清理时间：03:10
```

## 12. UCI 配置模型

配置文件：

```text
/etc/config/singboxlite
```

建议初始内容：

```text
config main 'main'
  option enabled '1'
  option mode 'singbox_mosdns'
  option config_path '/etc/sing-box/config.json'
  option log_path '/etc/sing-box/sing-box.log'
  option temp_dir '/tmp/singboxlite'
  option last_check_result ''
  option last_check_time ''
  option last_import_source ''
  option last_import_time ''
  option last_apply_time ''

config remote 'remote'
  option url ''
  option auto_update '0'
  option auto_update_time '03:00'
  option auto_apply '0'
  option last_update_time ''
  option last_update_result ''

config dns 'dns'
  option mosdns_addr '127.0.0.1'
  option mosdns_port '5335'
  option disable_dns_hijack '1'
  option restart_mosdns_after_apply '1'

config log 'log'
  option cleanup_enabled '1'
  option cleanup_time '03:10'
  option tail_lines '200'
```

## 13. 后端接口设计

建议使用 rpcd 暴露方法。

方法列表：

```text
status
check_current
upload_local
fetch_remote
check_imported
apply_imported
backup_current
restart_singbox
restart_mosdns
clean_log
tail_log
set_cron
get_config
set_config
```

### 13.1 status

返回：

```json
{
  "singbox_installed": true,
  "singbox_running": true,
  "singbox_version": "1.14.0-alpha.24",
  "singbox_pid": "23341",
  "mosdns_installed": true,
  "mosdns_running": true,
  "config_path": "/etc/sing-box/config.json",
  "log_path": "/etc/sing-box/sing-box.log",
  "log_size": 123456,
  "mode": "singbox_mosdns"
}
```

### 13.2 check_current

执行：

```sh
sing-box check -c /etc/sing-box/config.json
```

返回：

```json
{
  "ok": true,
  "output": ""
}
```

### 13.3 fetch_remote

输入：

```json
{
  "url": "https://example.com/sing-box.json"
}
```

返回：

```json
{
  "ok": true,
  "temp_path": "/tmp/singboxlite/import-remote.json",
  "message": "下载成功"
}
```

### 13.4 apply_imported

输入：

```json
{
  "source": "remote"
}
```

流程：

```text
根据 source 找到临时配置
再次执行 sing-box check
备份当前配置
覆盖正式配置
重启 sing-box
根据模式决定是否重启 MosDNS
返回结果
```

## 14. 安全应用流程

应用配置必须严格按这个顺序：

```text
1. 获取导入配置
2. 写入临时文件
3. 检查文件是否存在
4. 检查文件非空
5. 检查 JSON 合法
6. 执行 sing-box check
7. 检查通过后备份当前配置
8. 替换正式配置
9. 重启 sing-box
10. 检查 sing-box 是否运行
11. 根据模式处理 MosDNS
12. 返回最终结果
```

禁止：

```text
不检查直接覆盖正式配置
检查失败仍然应用
覆盖后不备份
失败时删除旧配置
```

## 15. 备份策略

备份路径格式：

```text
/etc/sing-box/config.json.bak-singboxlite-YYYYMMDD-HHMMSS
```

示例：

```text
/etc/sing-box/config.json.bak-singboxlite-20260517-013000
```

备份触发：

```text
应用本地配置前
应用远程配置前
手动点击备份配置
```

备份不自动删除。

## 16. 定时任务设计

需要管理两类 cron：

1. 远程配置自动更新。
2. 日志自动清理。

### 16.1 远程配置自动更新

示例：

```cron
0 3 * * * /usr/share/ucode/singboxlite.uc auto-update # singboxlite remote config update
```

自动更新必须：

```text
下载远程配置
执行 sing-box check
检查通过后才应用
失败则保留旧配置
记录结果
```

### 16.2 日志自动清理

示例：

```cron
10 3 * * * /usr/bin/truncate -s 0 /etc/sing-box/sing-box.log # clear sing-box log daily
```

## 17. rpcd ACL 权限

ACL 文件：

```text
/usr/share/rpcd/acl.d/luci-app-singbox-lite.json
```

权限原则：

- 只给需要的文件权限。
- 不给全盘写权限。
- 允许读取 sing-box 日志。
- 允许写入临时目录。
- 允许写入 sing-box 配置。
- 允许执行 sing-box。
- 允许执行 init.d 控制脚本。

需要读：

```text
/etc/config/singboxlite
/etc/sing-box/config.json
/etc/sing-box/sing-box.log
/usr/bin/sing-box
/etc/init.d/sing-box
/etc/init.d/mosdns
```

需要写：

```text
/etc/config/singboxlite
/etc/sing-box/config.json
/etc/sing-box/config.json.bak-*
/etc/sing-box/sing-box.log
/tmp/singboxlite/*
/etc/crontabs/root
```

需要执行：

```text
/usr/bin/sing-box
/etc/init.d/sing-box
/etc/init.d/mosdns
/usr/bin/truncate
/usr/bin/uclient-fetch
/usr/bin/wget
/usr/bin/curl
/usr/bin/sing-box-disable-dns-hijack
```

## 18. 错误处理

常见错误和提示：

### sing-box 未安装

提示：

```text
未找到 /usr/bin/sing-box，请先安装 sing-box。
```

### 配置检查失败

提示：

```text
配置检查失败，未应用新配置。
```

显示 `sing-box check` 输出。

### 远程 URL 下载失败

提示：

```text
远程配置下载失败，请检查 URL 或网络。
```

### MosDNS 未安装

提示：

```text
MosDNS 未安装，sing-box + mosdns 模式不可用。
```

### MosDNS 未运行

提示：

```text
MosDNS 当前未运行，可以尝试重启 MosDNS。
```

### 日志文件不存在

提示：

```text
日志文件不存在，可能 sing-box 尚未写入日志。
```

## 19. UI 文案

### 19.1 按钮

```text
上传并检查
拉取并检查
应用配置
检查配置
备份配置
重启 sing-box
重启 mosdns
清理日志
刷新日志
保存设置
```

### 19.2 状态

```text
运行中
未运行
未安装
检查通过
检查失败
已应用
未应用
下载成功
下载失败
已备份
已重启
```

### 19.3 模式

```text
sing-box + mosdns
仅 sing-box DNS
仅导入配置
```

## 20. UI 设计细节

总览页应保留状态仪表盘感，但不能复杂。

布局建议：

```text
顶部：页面标题 + 状态条
中部：左侧配置导入，右侧运行概况
底部：配置检查结果 + 最近日志
```

卡片不要嵌套卡片。

按钮使用图标加文字。

重要操作按钮：

```text
应用配置
重启 sing-box
```

要有明显主按钮样式。

危险操作：

```text
清理日志
停止 sing-box
```

要有确认提示。

## 21. 与 leosysd/ruleset 的关系

第一版不做复杂规则管理，但可以预留说明：

```text
如果用户导入的 JSON 已经引用 leosysd/ruleset，App 只负责检查和应用。
```

后续可以加：

```text
规则更新按钮
规则文件状态
更新后重启 sing-box
更新后重启 mosdns
```

但 V1 的核心不是规则管理。

## 22. 与现有 gfsingbox 的关系

当前路由器存在：

```text
/usr/lib/lua/luci/controller/gfsingbox.lua
/etc/config/gfsingbox
```

`SingBox Lite` 不直接修改 gfsingbox。

可选兼容：

```text
读取现有 /etc/sing-box/config.json
读取现有 sing-box 服务状态
不覆盖 gfsingbox 配置
不删除 gfsingbox 文件
```

## 23. 开发阶段计划

### 阶段 1：项目骨架

- 创建 LuCI App 目录。
- 创建 Makefile。
- 创建 controller。
- 创建 JS view。
- 创建 UCI 默认配置。
- 创建 rpcd ACL。

### 阶段 2：状态页

- 获取 sing-box 状态。
- 获取 MosDNS 状态。
- 获取版本。
- 获取日志大小。
- 显示当前模式。

### 阶段 3：配置导入

- 本地上传。
- 远程拉取。
- 临时文件保存。
- JSON 检查。
- sing-box check。

### 阶段 4：安全应用

- 备份当前配置。
- 替换正式配置。
- 重启 sing-box。
- 检查服务状态。
- MosDNS 联动。

### 阶段 5：日志

- 查看日志。
- 参考 GFSingBox，支持 Sing-box 日志、系统日志、软件日志三种来源。
- 支持级别过滤。
- 支持关键词搜索。
- 支持自动刷新。
- 支持原始日志查看。
- 清理日志。
- 自动清理 cron。

### 阶段 5.5：规则集

- 内置 `leosysd/ruleset` 路由器侧规则更新能力。
- 可设置 sing-box 规则目录。
- 可设置 MosDNS 规则目录。
- 可设置 `dist` 源地址。
- 可手动更新 6 个规则成品文件。
- 可写入每周自动更新 cron，默认周二 07:45。

### 阶段 6：打包和安装

- 生成 ipk。
- 安装测试。
- 卸载测试。
- 保留用户配置。

## 24. 验收标准

必须通过：

1. LuCI 菜单能打开 SingBox Lite。
2. 能看到 sing-box 当前运行状态。
3. 能上传本地 JSON。
4. 上传后能执行 `sing-box check`。
5. 检查失败时不会覆盖正式配置。
6. 检查成功后能应用配置。
7. 应用前会备份旧配置。
8. 能填写远程 URL 并拉取配置。
9. 远程配置也必须检查后才能应用。
10. 能选择 `sing-box + mosdns` 模式。
11. 能重启 sing-box。
12. 能重启 MosDNS。
13. 能查看日志。
14. 能清理日志。
15. 自动清理日志 cron 能写入并生效。
16. 能设置规则集输出目录。
17. 能手动更新 `direct/proxy` 的 sing-box 与 MosDNS 规则文件。
18. 规则集自动更新 cron 能写入并生效。

## 25. 最终原则

这个 App 要保持简单。

第一版只服务三个核心场景：

```text
我有一个本地 sing-box JSON，想安全导入到 OpenWrt。
我有一个远程 sing-box JSON URL，想安全拉取并应用。
我想选择 sing-box 和 MosDNS 如何配合运行。
```

所有功能都围绕这三个场景设计。
